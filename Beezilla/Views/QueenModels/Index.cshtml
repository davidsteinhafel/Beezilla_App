@model IEnumerable<Beezilla.Models.QueenModel>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Seen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Marked)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QueenStart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.QueenEnd)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Seen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Marked)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QueenStart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.QueenEnd)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
